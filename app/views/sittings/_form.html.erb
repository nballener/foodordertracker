<%= form_with(model: sitting, local: true) do |form| %>
  <% if sitting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sitting.errors.count, "error") %> prohibited this sitting from being saved:</h2>

      <ul>
      <% sitting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :sitting_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
